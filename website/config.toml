base_url = "https://gnaw.gitbadger.com"
title = "gnaw"
description = "Turn a codebase into an LLM-ready prompt, trimmed to a token budget."
default_language = "en"

compile_sass = true
build_search_index = true
generate_feeds = false
minify_html = false

[markdown]
smart_punctuation = false

# Zola >= 0.22 highlights via Giallo; config lives in [markdown.highlighting]
# and a theme is now required. Dual themes with inline style emit CSS
# light-dark() values, which we drive from `color-scheme` in main.scss so code
# colors follow the manual theme toggle. Verify these theme names against
# Giallo's built-in list: https://github.com/getzola/giallo#built-in
[markdown.highlighting]
style = "inline"
light_theme = "github-light"
dark_theme = "github-dark"

[extra]
github = "https://github.com/gitbadger-clan/gnaw"

# Sidebar groups — items can be:
#   - a leaf:               { title, url }
#   - an explicit subgroup: { title, items = [leaves] }
#   - an auto subgroup:     { title, auto = "<section>" }  (lists that section's pages)
# A top-level group may also use `auto` directly instead of `items`.
[[extra.sidebar]]
title = "Documentation 🚀"
[[extra.sidebar.items]]
title = "Tutorials"
auto = "tutorials"
[[extra.sidebar.items]]
title = "Explanations"
auto = "explanations"
[[extra.sidebar.items]]
title = "How-To Guides"
auto = "how-to"

[[extra.sidebar]]
title = "Overview"
[[extra.sidebar.items]]
title = "Welcome 👋"
url = "/"
[[extra.sidebar.items]]
title = "Vision 🔮"
url = "/vision/"
